# docker-compose.yml — run multiple OptionsOwl bots with shared config.
#
# Usage:
#   1. Edit .env with shared strategy/API config
#   2. Set PORTFOLIO_SIZE per bot below (only difference between owlets)
#   3. docker compose up -d
#
# Each bot gets:
#   - Shared .env config (mounted read-only)
#   - PORTFOLIO_SIZE override via environment
#   - Its own journal/ directory (SQLite DB, logs, heartbeat)

x-bot-common: &bot-common
  build: .
  restart: always
  stop_grace_period: 10s
  env_file: .env
  environment:
    - PYTHONUNBUFFERED=1
    # Default OFF for any service that INHERITS this anchor (e.g. darkpool-shadow): the HARVESTER is the
    # sole Polygon-WS holder so the account never exceeds Polygon's per-cluster connection limit. The
    # 5 trading bots set this explicitly in their own env; the harvester's own env block overrides this
    # (its collectors open WS regardless). Belt-and-suspenders so "harvester only" holds by config.
    - ENABLE_POLYGON_WS=false
  healthcheck:
    test: ["CMD", "python", "-c", "import time; from pathlib import Path; hb=Path('journal/heartbeat'); assert hb.exists(), 'no heartbeat'; age=time.time()-int(hb.read_text()); assert age<120, f'stale heartbeat: {age:.0f}s'"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 45s
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "5"
  deploy:
    resources:
      limits:
        memory: 1G

services:
  # ── Shared Redis (cross-agent coordination) ──
  redis:
    image: redis:7-alpine
    container_name: options-owl-redis
    restart: always
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - ./journal/redis-data:/data
    ports:
      - "127.0.0.1:6379:6379"
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ── Shared PostgreSQL (sourcing + future bot migration) ──
  postgres:
    image: postgres:16-alpine
    container_name: options-owl-db
    restart: always
    environment:
      POSTGRES_DB: options_owl
      POSTGRES_USER: owl
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-owl_dev_2026}
    volumes:
      - ./journal/postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U owl"]
      interval: 10s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  owlet-kody:
    <<: *bot-common
    container_name: owlet-kody
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=23000
      - FLEET_RANK=0
      - PAPER_TRADE=false
      - WEBULL_KILL_SWITCH=false
      - EXIT_ENGINE=v5
      # A1c canary (kody only for now): penny-match closed-trade P&L to real Webull fills.
      # Read-only vs Webull + updates P&L display columns only — proven isolated from the trading
      # path (17 tests incl. source-safety invariants). Expand to adam/dennis after Mon validation.
      - ENABLE_WEBULL_PNL_RECONCILE=true
      # Account-type toggle. false = Individual CASH (current; GFV-limited intraday reinvest).
      # true = Individual MARGIN: auto-detects the margin account (UNLESS WEBULL_ACCOUNT_ID is pinned —
      # kody is, so update its ID too) + skips GFV so sale proceeds reinvest intraday. Sizes off net-liq
      # (no extra leverage). PDT no longer a constraint. Flip per-bot when ready, then redeploy that bot.
      - MARGIN_ACCOUNT=false
      # Data-freshness guard: halt NEW entries + alert if the harvester feed goes stale during market
      # hours (catches frozen-but-fresh-timestamp data). Sell path unaffected; fails OPEN on error.
      - ENABLE_DATA_FRESHNESS_GUARD=true
      # Flat premium cap on FLOW CALLS — skip flow calls with entry premium > $9 (expensive flow calls
      # are net losers; validated +$3.3k/64d, PF 1.40->1.50; blocks the LRCX-type −$445 tail). 2026-06-24.
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      # Flow-put market-direction filter (2026-07-01): block flow INDEX puts when SPY is up >0.5%
      # from the open (counter-trend — the SPY-put -54% case). Validated: SPY puts +$6,326/PF 1.32→1.59.
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      # Delta-sized budget (cheap-OTM-call tail brake, calls-only, ref 0.45) — pure risk-reduction, all bots.
      - ENABLE_DELTA_SIZED_BUDGET=true
      # FOMC-day pause: block all new entries on Fed announcement days (Trump-Fed panic days hurt both sides) — 2026-06-18.
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      - WEBULL_APP_KEY=${KODY_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${KODY_WEBULL_APP_SECRET}
      - WEBULL_ACCOUNT_ID=${KODY_WEBULL_ACCOUNT_ID}
      - WEBULL_EMAIL=${KODY_WEBULL_EMAIL}
      - WEBULL_PASSWORD=${KODY_WEBULL_PASSWORD}
      - WEBULL_TRADE_PIN=${KODY_WEBULL_TRADE_PIN}
      - WEBULL_DID=${KODY_WEBULL_DID}
      # Signal source: sourcing pipeline only (no Discord signals)
      - ENABLE_DISCORD_SIGNALS=false
      # V6 enhancements
      - ENABLE_V6_PER_TICKER_CONFIG=true
      # V7 Phase A (CALL wide-trail exits) — kody only, staged rollout 2026-06-12.
      # Exit-only ablation: both OOS windows beat V6 exits at identical win rate
      # (OOS +72% / OOS2 +27% P&L, higher PF). PUTs unchanged.
      - ENABLE_V7_WIDE_TRAIL=true
      # V7 gate-volume fix (multi-value sweep 2026-06-13, 3 OOS windows): pattern 0.74→0.62
      # at entry 0.80. ~1.8× volume (1.7→3.1 trades/day), PF holds ~3.8, DD 6.5→10.
      - ML_PATTERN_THRESHOLD=0.62
      # UW whale-flow signals on all bots 2026-06-13 (live on kody/dennis, paper on adam/vinny/yank).
      - ENABLE_UW_FLOW_SIGNAL=true
      # Selective OTM flow strike (validated combos: AMD/INTC/META/SPY calls, TSLA puts) — 2026-06-15.
      - ENABLE_FLOW_OTM_STRIKE=true
      # Anti-martingale ADD: buy more on a confirmed runner (CALL +30 / PUT +30&+100 pyramid) — 2026-06-15.
      # Anti-martingale add OFF (2026-06-16): flat-+EV but it DILUTES book PF + raises drawdown →
      # hurts compounding ($18k/60d gold standard: $2.1M lock-only → $0.6M with adds). Separate-leg
      # + safety code stays in place, dormant. Re-enable only with a compounding-positive variant.
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      # CALL-only profit-lock (keep 60% of peak gain once +30%) — the runner-capture win, stays ON.
      - ENABLE_V7_PROFIT_LOCK=true
      # 0DTE premium hard-stop -25%: cut a 0DTE contract down 25% from entry regardless of underlying
      # (the NVDA -42% theta-melt fix) — PUT PF win + call tail insurance, 2026-06-16.
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      # Multi-day PUT -25% hardstop — fleet-wide 2026-07-02 (Kody's call: small EV cost for a tighter
      # floor). Risk-control, EV-negative by backtest (-$690/3mo); 0DTE puts already cut by the gate above.
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      # 2026-07-01 late-day/overnight risk controls (all fleet-wide, env-gated):
      #  • STALL_CUT: cut dead-on-arrival multi-day legs (>30m held, -30%, peaked <10%) — kills the
      #    1-DTE bleeder that rode the wide 50% backstop for an hour (adam META). Validated ~neutral.
      #  • EOD_CLOSE_ALL: force-close multi-day at the EOD cutoff — no overnight gap risk.
      #  • LATE_ENTRY_CUTOFF: no new entries after 14:30 ET (last 90 min) — live fills show 14:00-16:00
      #    is net-negative (Kody's TSLA last-min burns). Applies to ALL sources incl. flow.
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      # Take-profit: freeze sizing at $50k once reached, bank the excess (caps absolute drawdown) — 2026-06-16.
      - MAX_SIZING_BALANCE=50000
      # Fleet staggering K=2: each signal → 2 of 5 bots (priority round-robin) so they don't all crater
      # together (corr 0.95→0.22, fleet DD 5x smaller) — 2026-06-16. FLEET_RANK is per-bot (below).
      # DISABLED 2026-06-23: stagger decorrelated MULTIPLE live bots, but kody is the only live bot now
      # (dennis -> paper). Splitting the pool just handed kody a random sub-slice of the edge (it drew the
      # -$537 corner of a +$1,863 green fleet day while adam took the winners). Off = every bot evaluates
      # EVERY signal concurrently; kody (FLEET_RANK=0) captures the full edge instead of a fraction.
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      # B2 market-tide gate: down-size puts entered against the market-wide whale tide (puts-only) — 2026-06-15.
      - ENABLE_V7_TIDE_GATE=true
      # Stage D conviction sizing — kody LIVE 2026-06-14 (validated +72% / combined PF 1.78; vinny paper canary).
      - ENABLE_V7_CONVICTION_SIZING=true
      # runner_v1 P(runner) call-sizing — CONSERVATIVE-LIVE 2026-06-22. Backtest-validated (~2x per-unit P&L,
      # monotonic P(runner)->outcome); serve path verified on adam/vinny canary (sane 0.60-0.73 scores). Softened
      # 0.7-1.3 mults (not the full 0.5-1.75 curve). Small paper bots couldn't test sizing (1 contract = no-op);
      # the edge only bites on multi-contract accounts (kody/dennis). 100% harvester-sourced (no per-bot Polygon).
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      # conf_linear (winner-concentration) sizing — AGGRESSIVE 0.4-1.8, 6mo-validated 2026-06-22:
      # +107% P&L vs flat at LOWER drawdown over 254 trades. Stacks w/ runner_v1 (×0.28–×2.34);
      # MAX_POSITION_PCT/MAX_POSITION_DOLLARS caps bound the upside. Live kody, paper shadows on the rest.
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true
      # Polygon WS disabled — Kody's key is used by harvester for centralized flow capture.
      # Premium monitoring uses Polygon REST (accurate, ~1s delay). All agents get
      # real-time flow data from harvester via Redis pub/sub.
      - ENABLE_POLYGON_WS=false
      - AGENT_ID=owlet_kody
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-kody:/app/journal
      - ./journal/models:/app/journal/models:ro

  owlet-dennis:
    <<: *bot-common
    container_name: owlet-dennis
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=10000
      - FLEET_RANK=2
      # dennis → LIVE 2026-06-30 (Kody's call — re-auth Webull; 2nd live-money bot after kody+adam, $10k).
      - PAPER_TRADE=false
      - WEBULL_KILL_SWITCH=false
      - EXIT_ENGINE=v5
      # Account-type toggle. false = Individual CASH (current; GFV-limited intraday reinvest).
      # true = Individual MARGIN: auto-detects the margin account (UNLESS WEBULL_ACCOUNT_ID is pinned —
      # kody is, so update its ID too) + skips GFV so sale proceeds reinvest intraday. Sizes off net-liq
      # (no extra leverage). PDT no longer a constraint. Flip per-bot when ready, then redeploy that bot.
      - MARGIN_ACCOUNT=false
      # Data-freshness guard: halt NEW entries + alert if the harvester feed goes stale during market
      # hours (catches frozen-but-fresh-timestamp data). Sell path unaffected; fails OPEN on error.
      - ENABLE_DATA_FRESHNESS_GUARD=true
      # Flat premium cap on FLOW CALLS — skip flow calls with entry premium > $9 (expensive flow calls
      # are net losers; validated +$3.3k/64d, PF 1.40->1.50; blocks the LRCX-type −$445 tail). 2026-06-24.
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      # Flow-put market-direction filter (2026-07-01): block flow INDEX puts when SPY is up >0.5%
      # from the open (counter-trend — the SPY-put -54% case). Validated: SPY puts +$6,326/PF 1.32→1.59.
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      # Delta-sized budget (cheap-OTM-call tail brake, calls-only, ref 0.45) — pure risk-reduction, all bots.
      - ENABLE_DELTA_SIZED_BUDGET=true
      # FOMC-day pause: block all new entries on Fed announcement days (Trump-Fed panic days hurt both sides) — 2026-06-18.
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      - WEBULL_APP_KEY=${DENNIS_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${DENNIS_WEBULL_APP_SECRET}
      # Signal source: sourcing pipeline only (no Discord signals)
      - ENABLE_DISCORD_SIGNALS=false
      # V6 enhancements
      - ENABLE_V6_PER_TICKER_CONFIG=true
      # V7 wide-trail exits (CALL + PUT) — enabled for dennis 2026-06-12
      - ENABLE_V7_WIDE_TRAIL=true
      # V7 gate-volume fix (multi-value sweep 2026-06-13, 3 OOS windows): pattern 0.74→0.62
      # at entry 0.80. ~1.8× volume (1.7→3.1 trades/day), PF holds ~3.8, DD 6.5→10.
      - ML_PATTERN_THRESHOLD=0.62
      # UW whale-flow signals on all bots 2026-06-13 (live on kody/dennis, paper on adam/vinny/yank).
      - ENABLE_UW_FLOW_SIGNAL=true
      # Selective OTM flow strike (validated combos: AMD/INTC/META/SPY calls, TSLA puts) — 2026-06-15.
      - ENABLE_FLOW_OTM_STRIKE=true
      # Anti-martingale ADD: buy more on a confirmed runner (CALL +30 / PUT +30&+100 pyramid) — 2026-06-15.
      # Anti-martingale add OFF (2026-06-16): flat-+EV but it DILUTES book PF + raises drawdown →
      # hurts compounding ($18k/60d gold standard: $2.1M lock-only → $0.6M with adds). Separate-leg
      # + safety code stays in place, dormant. Re-enable only with a compounding-positive variant.
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      # CALL-only profit-lock (keep 60% of peak gain once +30%) — the runner-capture win, stays ON.
      - ENABLE_V7_PROFIT_LOCK=true
      # 0DTE premium hard-stop -25%: cut a 0DTE contract down 25% from entry regardless of underlying
      # (the NVDA -42% theta-melt fix) — PUT PF win + call tail insurance, 2026-06-16.
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      # Multi-day PUT -25% hardstop — fleet-wide 2026-07-02 (Kody's call: small EV cost for a tighter
      # floor). Risk-control, EV-negative by backtest (-$690/3mo); 0DTE puts already cut by the gate above.
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      # 2026-07-01 late-day/overnight risk controls (all fleet-wide, env-gated):
      #  • STALL_CUT: cut dead-on-arrival multi-day legs (>30m held, -30%, peaked <10%) — kills the
      #    1-DTE bleeder that rode the wide 50% backstop for an hour (adam META). Validated ~neutral.
      #  • EOD_CLOSE_ALL: force-close multi-day at the EOD cutoff — no overnight gap risk.
      #  • LATE_ENTRY_CUTOFF: no new entries after 14:30 ET (last 90 min) — live fills show 14:00-16:00
      #    is net-negative (Kody's TSLA last-min burns). Applies to ALL sources incl. flow.
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      # Take-profit: freeze sizing at $50k once reached, bank the excess (caps absolute drawdown) — 2026-06-16.
      - MAX_SIZING_BALANCE=50000
      # Fleet staggering K=2: each signal → 2 of 5 bots (priority round-robin) so they don't all crater
      # together (corr 0.95→0.22, fleet DD 5x smaller) — 2026-06-16. FLEET_RANK is per-bot (below).
      # DISABLED 2026-06-23: stagger decorrelated MULTIPLE live bots, but kody is the only live bot now
      # (dennis -> paper). Splitting the pool just handed kody a random sub-slice of the edge (it drew the
      # -$537 corner of a +$1,863 green fleet day while adam took the winners). Off = every bot evaluates
      # EVERY signal concurrently; kody (FLEET_RANK=0) captures the full edge instead of a fraction.
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      # B2 market-tide gate: down-size puts entered against the market-wide whale tide (puts-only) — 2026-06-15.
      - ENABLE_V7_TIDE_GATE=true
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true
      - ENABLE_POLYGON_WS=false
      - ENABLE_V7_CONVICTION_SIZING=true
      # runner_v1 P(runner) call-sizing — CONSERVATIVE-LIVE 2026-06-22 (see kody block for rationale).
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      # conf_linear (winner-concentration) sizing — AGGRESSIVE 0.4-1.8, 6mo-validated 2026-06-22:
      # +107% P&L vs flat at LOWER drawdown over 254 trades. Stacks w/ runner_v1 (×0.28–×2.34);
      # MAX_POSITION_PCT/MAX_POSITION_DOLLARS caps bound the upside. Live kody, paper shadows on the rest.
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - AGENT_ID=owlet_dennis
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-dennis:/app/journal
      - ./journal/models:/app/journal/models:ro

  owlet-alan:
    <<: *bot-common
    container_name: owlet-alan
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=10000
      - FLEET_RANK=5
      # alan → PAPER 2026-07-05 (Kody's call — flipped to paper shadow; margin account funded only $100).
      # Was briefly LIVE on deploy 2026-07-04; run as paper until funded, then flip both flags back.
      - PAPER_TRADE=true
      - WEBULL_KILL_SWITCH=true
      - EXIT_ENGINE=v5
      # Individual MARGIN: auto-detects the margin account (WEBULL_ACCOUNT_ID unpinned) + skips GFV so
      # sale proceeds reinvest intraday. Sizes off net-liq (no extra leverage). PDT not a constraint.
      - MARGIN_ACCOUNT=true
      - ENABLE_DATA_FRESHNESS_GUARD=true
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      - ENABLE_DELTA_SIZED_BUDGET=true
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      - WEBULL_APP_KEY=${ALAN_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${ALAN_WEBULL_APP_SECRET}
      - ENABLE_DISCORD_SIGNALS=false
      - ENABLE_V6_PER_TICKER_CONFIG=true
      - ENABLE_V7_WIDE_TRAIL=true
      - ML_PATTERN_THRESHOLD=0.62
      - ENABLE_UW_FLOW_SIGNAL=true
      - ENABLE_FLOW_OTM_STRIKE=true
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      - ENABLE_V7_PROFIT_LOCK=true
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      - MAX_SIZING_BALANCE=50000
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      - ENABLE_V7_TIDE_GATE=true
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true
      - ENABLE_POLYGON_WS=false
      - ENABLE_V7_CONVICTION_SIZING=true
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - AGENT_ID=owlet_alan
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-alan:/app/journal
      - ./journal/models:/app/journal/models:ro

  owlet-adam:
    <<: *bot-common
    container_name: owlet-adam
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=4685
      # runner_v1 P(runner) CALL sizing (paper canary): bet bigger on high-P(runner) rippers. Cuts = live
      # harvester-distribution percentiles (probe 2026-06-18, n=81: p25/50/75 = 0.39/0.69/0.85); softened
      # multipliers (0.7-1.3 vs validated 0.5-1.5) while the harvester-greek separation is validated forward
      # on paper before live (kody/dennis). Scorer is 100% harvester-sourced (no per-bot Polygon).
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      # conf_linear (winner-concentration) sizing — AGGRESSIVE 0.4-1.8, 6mo-validated 2026-06-22:
      # +107% P&L vs flat at LOWER drawdown over 254 trades. Stacks w/ runner_v1 (×0.28–×2.34);
      # MAX_POSITION_PCT/MAX_POSITION_DOLLARS caps bound the upside. Live kody, paper shadows on the rest.
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - FLEET_RANK=1
      - POLYGON_API_KEY=${ADAM_POLYGON_API_KEY}
      - ENABLE_POLYGON_WS=false
      - WEBULL_APP_KEY=${ADAM_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${ADAM_WEBULL_APP_SECRET}
      # adam → back to PAPER 2026-07-09 (Kody's call — reverted from the 06-23 live-margin run).
      # MARGIN_ACCOUNT=true auto-detects adam's Individual MARGIN account; skips GFV → reinvest intraday.
      - PAPER_TRADE=true
      - WEBULL_KILL_SWITCH=true
      - EXIT_ENGINE=v5
      # Account-type toggle. false = Individual CASH; true = Individual MARGIN (auto-detect unless
      # WEBULL_ACCOUNT_ID pinned) + skips GFV so sale proceeds reinvest intraday. Sizes off net-liq (no
      # extra leverage). PDT no longer a constraint. Flip per-bot when ready, then redeploy that bot.
      - MARGIN_ACCOUNT=true
      # Data-freshness guard: halt NEW entries + alert if the harvester feed goes stale during market
      # hours (catches frozen-but-fresh-timestamp data). Sell path unaffected; fails OPEN on error.
      - ENABLE_DATA_FRESHNESS_GUARD=true
      # Flat premium cap on FLOW CALLS — skip flow calls with entry premium > $9 (expensive flow calls
      # are net losers; validated +$3.3k/64d, PF 1.40->1.50; blocks the LRCX-type −$445 tail). 2026-06-24.
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      # Flow-put market-direction filter (2026-07-01): block flow INDEX puts when SPY is up >0.5%
      # from the open (counter-trend — the SPY-put -54% case). Validated: SPY puts +$6,326/PF 1.32→1.59.
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      # Delta-sized budget (cheap-OTM-call tail brake, calls-only, ref 0.45) — pure risk-reduction, all bots.
      - ENABLE_DELTA_SIZED_BUDGET=true
      # FOMC-day pause: block all new entries on Fed announcement days (Trump-Fed panic days hurt both sides) — 2026-06-18.
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      # Signal source: sourcing pipeline only (no Discord signals)
      - ENABLE_DISCORD_SIGNALS=false
      # V6 enhancements
      - ENABLE_V6_PER_TICKER_CONFIG=true
      # Same V7 strategy as kody/dennis (identical code+config; only PAPER_TRADE differs).
      - ENABLE_V7_WIDE_TRAIL=true
      - ML_PATTERN_THRESHOLD=0.62
      # UW whale-flow signals on all bots 2026-06-13 (live on kody/dennis, paper on adam/vinny/yank).
      - ENABLE_UW_FLOW_SIGNAL=true
      # Selective OTM flow strike (validated combos: AMD/INTC/META/SPY calls, TSLA puts) — 2026-06-15.
      - ENABLE_FLOW_OTM_STRIKE=true
      # Anti-martingale ADD: buy more on a confirmed runner (CALL +30 / PUT +30&+100 pyramid) — 2026-06-15.
      # Anti-martingale add OFF (2026-06-16): flat-+EV but it DILUTES book PF + raises drawdown →
      # hurts compounding ($18k/60d gold standard: $2.1M lock-only → $0.6M with adds). Separate-leg
      # + safety code stays in place, dormant. Re-enable only with a compounding-positive variant.
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      # CALL-only profit-lock (keep 60% of peak gain once +30%) — the runner-capture win, stays ON.
      - ENABLE_V7_PROFIT_LOCK=true
      # 0DTE premium hard-stop -25%: cut a 0DTE contract down 25% from entry regardless of underlying
      # (the NVDA -42% theta-melt fix) — PUT PF win + call tail insurance, 2026-06-16.
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      # Multi-day PUT -25% hardstop — fleet-wide 2026-07-02 (Kody's call: small EV cost for a tighter
      # floor). Risk-control, EV-negative by backtest (-$690/3mo); 0DTE puts already cut by the gate above.
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      # 2026-07-01 late-day/overnight risk controls (all fleet-wide, env-gated):
      #  • STALL_CUT: cut dead-on-arrival multi-day legs (>30m held, -30%, peaked <10%) — kills the
      #    1-DTE bleeder that rode the wide 50% backstop for an hour (adam META). Validated ~neutral.
      #  • EOD_CLOSE_ALL: force-close multi-day at the EOD cutoff — no overnight gap risk.
      #  • LATE_ENTRY_CUTOFF: no new entries after 14:30 ET (last 90 min) — live fills show 14:00-16:00
      #    is net-negative (Kody's TSLA last-min burns). Applies to ALL sources incl. flow.
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      # Take-profit: freeze sizing at $50k once reached, bank the excess (caps absolute drawdown) — 2026-06-16.
      - MAX_SIZING_BALANCE=50000
      # Fleet staggering K=2: each signal → 2 of 5 bots (priority round-robin) so they don't all crater
      # together (corr 0.95→0.22, fleet DD 5x smaller) — 2026-06-16. FLEET_RANK is per-bot (below).
      # DISABLED 2026-06-23: stagger decorrelated MULTIPLE live bots, but kody is the only live bot now
      # (dennis -> paper). Splitting the pool just handed kody a random sub-slice of the edge (it drew the
      # -$537 corner of a +$1,863 green fleet day while adam took the winners). Off = every bot evaluates
      # EVERY signal concurrently; kody (FLEET_RANK=0) captures the full edge instead of a fraction.
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      # B2 market-tide gate: down-size puts entered against the market-wide whale tide (puts-only) — 2026-06-15.
      - ENABLE_V7_TIDE_GATE=true
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true

      - ENABLE_V7_CONVICTION_SIZING=true
      - AGENT_ID=owlet_adam
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-adam:/app/journal
      - ./journal/models:/app/journal/models:ro

  # TIGHT-LOCK PAPER CANARY (vinny, 2026-06-29) — "lock the win, don't bleed it back".
  # Keep 80% of peak (not 60%), arm at +25% (not +30%), and apply profit-lock to PUTs too.
  # Paper only — observe vs the other bots before any live change.
  owlet-vinny:
    <<: *bot-common
    container_name: owlet-vinny
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=3123
      # runner_v1 P(runner) CALL sizing (paper canary): bet bigger on high-P(runner) rippers. Cuts = live
      # harvester-distribution percentiles (probe 2026-06-18, n=81: p25/50/75 = 0.39/0.69/0.85); softened
      # multipliers (0.7-1.3 vs validated 0.5-1.5) while the harvester-greek separation is validated forward
      # on paper before live (kody/dennis). Scorer is 100% harvester-sourced (no per-bot Polygon).
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      # conf_linear (winner-concentration) sizing — AGGRESSIVE 0.4-1.8, 6mo-validated 2026-06-22:
      # +107% P&L vs flat at LOWER drawdown over 254 trades. Stacks w/ runner_v1 (×0.28–×2.34);
      # MAX_POSITION_PCT/MAX_POSITION_DOLLARS caps bound the upside. Live kody, paper shadows on the rest.
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - FLEET_RANK=4
      - POLYGON_API_KEY=${VINNY_POLYGON_API_KEY}
      - ENABLE_POLYGON_WS=false
      - WEBULL_APP_KEY=${VINNY_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${VINNY_WEBULL_APP_SECRET}
      # vinny → back to PAPER 2026-07-09 (Kody's call — reverted from the 07-01 live-margin run).
      # WEBULL_ACCOUNT_ID unpinned, so MARGIN_ACCOUNT=true auto-detects vinny's Individual MARGIN account.
      - PAPER_TRADE=true
      - WEBULL_KILL_SWITCH=true
      - EXIT_ENGINE=v5
      # Account-type toggle. false = Individual CASH (GFV-limited intraday reinvest).
      # true = Individual MARGIN: auto-detects the margin account (UNLESS WEBULL_ACCOUNT_ID is pinned)
      # + skips GFV so sale proceeds reinvest intraday. Sizes off net-liq (no extra leverage).
      - MARGIN_ACCOUNT=true
      # CANARY (2026-07-02): fast tiered EXIT chase — sub-cycle repricing off the Webull venue bid,
      # same double-fill safety as the entry chase. vinny = smallest live account; validate here before
      # fleet-wide. Others stay false (legacy venue-priced escalation, already fixed).
      - ENABLE_FAST_EXIT_CHASE=true
      # Data-freshness guard: halt NEW entries + alert if the harvester feed goes stale during market
      # hours (catches frozen-but-fresh-timestamp data). Sell path unaffected; fails OPEN on error.
      - ENABLE_DATA_FRESHNESS_GUARD=true
      # Flat premium cap on FLOW CALLS — skip flow calls with entry premium > $9 (expensive flow calls
      # are net losers; validated +$3.3k/64d, PF 1.40->1.50; blocks the LRCX-type −$445 tail). 2026-06-24.
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      # Flow-put market-direction filter (2026-07-01): block flow INDEX puts when SPY is up >0.5%
      # from the open (counter-trend — the SPY-put -54% case). Validated: SPY puts +$6,326/PF 1.32→1.59.
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      # Delta-sized budget (cheap-OTM-call tail brake, calls-only, ref 0.45) — pure risk-reduction, all bots.
      - ENABLE_DELTA_SIZED_BUDGET=true
      # FOMC-day pause: block all new entries on Fed announcement days (Trump-Fed panic days hurt both sides) — 2026-06-18.
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      # Signal source: sourcing pipeline only (no Discord signals)
      - ENABLE_DISCORD_SIGNALS=false
      # V6 enhancements
      - ENABLE_V6_PER_TICKER_CONFIG=true
      # Same V7 strategy as kody/dennis (identical code+config; only PAPER_TRADE differs).
      - ENABLE_V7_WIDE_TRAIL=true
      - ML_PATTERN_THRESHOLD=0.62
      # UW whale-flow signals on all bots 2026-06-13 (live on kody/dennis, paper on adam/vinny/yank).
      - ENABLE_UW_FLOW_SIGNAL=true
      # Selective OTM flow strike (validated combos: AMD/INTC/META/SPY calls, TSLA puts) — 2026-06-15.
      - ENABLE_FLOW_OTM_STRIKE=true
      # Anti-martingale ADD: buy more on a confirmed runner (CALL +30 / PUT +30&+100 pyramid) — 2026-06-15.
      # Anti-martingale add OFF (2026-06-16): flat-+EV but it DILUTES book PF + raises drawdown →
      # hurts compounding ($18k/60d gold standard: $2.1M lock-only → $0.6M with adds). Separate-leg
      # + safety code stays in place, dormant. Re-enable only with a compounding-positive variant.
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      # CALL-only profit-lock (keep 60% of peak gain once +30%) — the runner-capture win, stays ON.
      - V7_PROFIT_LOCK_KEEP_FRAC=0.8
      - V7_PROFIT_LOCK_ACTIVATE_PCT=25
      - V7_PROFIT_LOCK_PUTS=true
      - ENABLE_V7_PROFIT_LOCK=true
      # 0DTE premium hard-stop -25%: cut a 0DTE contract down 25% from entry regardless of underlying
      # (the NVDA -42% theta-melt fix) — PUT PF win + call tail insurance, 2026-06-16.
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      # Multi-day PUT -25% hardstop — fleet-wide 2026-07-02 (Kody's call: small EV cost for a tighter
      # floor). Risk-control, EV-negative by backtest (-$690/3mo); 0DTE puts already cut by the gate above.
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      # 2026-07-01 late-day/overnight risk controls (all fleet-wide, env-gated):
      #  • STALL_CUT: cut dead-on-arrival multi-day legs (>30m held, -30%, peaked <10%) — kills the
      #    1-DTE bleeder that rode the wide 50% backstop for an hour (adam META). Validated ~neutral.
      #  • EOD_CLOSE_ALL: force-close multi-day at the EOD cutoff — no overnight gap risk.
      #  • LATE_ENTRY_CUTOFF: no new entries after 14:30 ET (last 90 min) — live fills show 14:00-16:00
      #    is net-negative (Kody's TSLA last-min burns). Applies to ALL sources incl. flow.
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      # Take-profit: freeze sizing at $50k once reached, bank the excess (caps absolute drawdown) — 2026-06-16.
      - MAX_SIZING_BALANCE=50000
      # Fleet staggering K=2: each signal → 2 of 5 bots (priority round-robin) so they don't all crater
      # together (corr 0.95→0.22, fleet DD 5x smaller) — 2026-06-16. FLEET_RANK is per-bot (below).
      # DISABLED 2026-06-23: stagger decorrelated MULTIPLE live bots, but kody is the only live bot now
      # (dennis -> paper). Splitting the pool just handed kody a random sub-slice of the edge (it drew the
      # -$537 corner of a +$1,863 green fleet day while adam took the winners). Off = every bot evaluates
      # EVERY signal concurrently; kody (FLEET_RANK=0) captures the full edge instead of a fraction.
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      # B2 market-tide gate: down-size puts entered against the market-wide whale tide (puts-only) — 2026-06-15.
      - ENABLE_V7_TIDE_GATE=true
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      # Stage D conviction sizing — vinny FIRST (paper canary), validated +72%/PF1.64; then kody.
      - ENABLE_V7_CONVICTION_SIZING=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true

      - AGENT_ID=owlet_vinny
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-vinny:/app/journal
      - ./journal/models:/app/journal/models:ro

  owlet-yank:
    <<: *bot-common
    container_name: owlet-yank
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - PORTFOLIO_SIZE=3600
      - FLEET_RANK=3
      - POLYGON_API_KEY=${YANK_POLYGON_API_KEY}
      - ENABLE_POLYGON_WS=false
      - WEBULL_APP_KEY=${YANK_WEBULL_APP_KEY}
      - WEBULL_APP_SECRET=${YANK_WEBULL_APP_SECRET}
      - PAPER_TRADE=true
      - WEBULL_KILL_SWITCH=true
      - EXIT_ENGINE=v5
      # Account-type toggle. false = Individual CASH (current; GFV-limited intraday reinvest).
      # true = Individual MARGIN: auto-detects the margin account (UNLESS WEBULL_ACCOUNT_ID is pinned —
      # kody is, so update its ID too) + skips GFV so sale proceeds reinvest intraday. Sizes off net-liq
      # (no extra leverage). PDT no longer a constraint. Flip per-bot when ready, then redeploy that bot.
      - MARGIN_ACCOUNT=false
      # Data-freshness guard: halt NEW entries + alert if the harvester feed goes stale during market
      # hours (catches frozen-but-fresh-timestamp data). Sell path unaffected; fails OPEN on error.
      - ENABLE_DATA_FRESHNESS_GUARD=true
      # Flat premium cap on FLOW CALLS — skip flow calls with entry premium > $9 (expensive flow calls
      # are net losers; validated +$3.3k/64d, PF 1.40->1.50; blocks the LRCX-type −$445 tail). 2026-06-24.
      - FLOW_CALL_MAX_PREMIUM=9.0
      - FLOW_PUT_MAX_PREMIUM=9.0
      # Flow-put market-direction filter (2026-07-01): block flow INDEX puts when SPY is up >0.5%
      # from the open (counter-trend — the SPY-put -54% case). Validated: SPY puts +$6,326/PF 1.32→1.59.
      - ENABLE_FLOW_PUT_MKT_DIR=true
      - ENABLE_FLOW_CALL_MKT_DIR=true
      - FLOW_PUT_MKT_DIR_MAX_CHG=0.5
      # Delta-sized budget (cheap-OTM-call tail brake, calls-only, ref 0.45) — pure risk-reduction, all bots.
      - ENABLE_DELTA_SIZED_BUDGET=true
      # FOMC-day pause: block all new entries on Fed announcement days (Trump-Fed panic days hurt both sides) — 2026-06-18.
      - ENABLE_FOMC_PAUSE=true
      - MAX_CONCURRENT=8
      - MAX_POSITION_PCT=15
      - MAX_DCA_POSITION_PCT=10
      - MAX_TRADE_LOSS_EXIT_PCT=8
      - MAX_PORTFOLIO_RISK_PCT=75
      - ENABLE_WEBULL_PAPER=true
      # Signal source: sourcing pipeline only (no Discord signals)
      - ENABLE_DISCORD_SIGNALS=false
      # V6 enhancements
      - ENABLE_V6_PER_TICKER_CONFIG=true
      # Same V7 strategy as kody/dennis (identical code+config; only PAPER_TRADE differs).
      - ENABLE_V7_WIDE_TRAIL=true
      - ML_PATTERN_THRESHOLD=0.62
      # UW whale-flow signals on all bots 2026-06-13 (live on kody/dennis, paper on adam/vinny/yank).
      - ENABLE_UW_FLOW_SIGNAL=true
      # Selective OTM flow strike (validated combos: AMD/INTC/META/SPY calls, TSLA puts) — 2026-06-15.
      - ENABLE_FLOW_OTM_STRIKE=true
      # Anti-martingale ADD: buy more on a confirmed runner (CALL +30 / PUT +30&+100 pyramid) — 2026-06-15.
      # Anti-martingale add OFF (2026-06-16): flat-+EV but it DILUTES book PF + raises drawdown →
      # hurts compounding ($18k/60d gold standard: $2.1M lock-only → $0.6M with adds). Separate-leg
      # + safety code stays in place, dormant. Re-enable only with a compounding-positive variant.
      - ENABLE_ANTIMARTINGALE_ADD=false
      - ANTIMG_CALL_LEVELS=30,80,150
      # CALL-only profit-lock (keep 60% of peak gain once +30%) — the runner-capture win, stays ON.
      - ENABLE_V7_PROFIT_LOCK=true
      # 0DTE premium hard-stop -25%: cut a 0DTE contract down 25% from entry regardless of underlying
      # (the NVDA -42% theta-melt fix) — PUT PF win + call tail insurance, 2026-06-16.
      - ENABLE_0DTE_PREMIUM_HARDSTOP=true
      - ENABLE_MULTIDAY_CALL_HARDSTOP=true
      # Multi-day PUT -25% hardstop — fleet-wide 2026-07-02 (Kody's call: small EV cost for a tighter
      # floor). Risk-control, EV-negative by backtest (-$690/3mo); 0DTE puts already cut by the gate above.
      - ENABLE_MULTIDAY_PUT_HARDSTOP=true
      # 2026-07-01 late-day/overnight risk controls (all fleet-wide, env-gated):
      #  • STALL_CUT: cut dead-on-arrival multi-day legs (>30m held, -30%, peaked <10%) — kills the
      #    1-DTE bleeder that rode the wide 50% backstop for an hour (adam META). Validated ~neutral.
      #  • EOD_CLOSE_ALL: force-close multi-day at the EOD cutoff — no overnight gap risk.
      #  • LATE_ENTRY_CUTOFF: no new entries after 14:30 ET (last 90 min) — live fills show 14:00-16:00
      #    is net-negative (Kody's TSLA last-min burns). Applies to ALL sources incl. flow.
      - ENABLE_STALL_CUT=true
      - ENABLE_EOD_CLOSE_ALL=true
      - ENABLE_LATE_ENTRY_CUTOFF=true
      - PREMIUM_HARDSTOP_0DTE_PCT=25
      # Take-profit: freeze sizing at $50k once reached, bank the excess (caps absolute drawdown) — 2026-06-16.
      - MAX_SIZING_BALANCE=50000
      # Fleet staggering K=2: each signal → 2 of 5 bots (priority round-robin) so they don't all crater
      # together (corr 0.95→0.22, fleet DD 5x smaller) — 2026-06-16. FLEET_RANK is per-bot (below).
      # DISABLED 2026-06-23: stagger decorrelated MULTIPLE live bots, but kody is the only live bot now
      # (dennis -> paper). Splitting the pool just handed kody a random sub-slice of the edge (it drew the
      # -$537 corner of a +$1,863 green fleet day while adam took the winners). Off = every bot evaluates
      # EVERY signal concurrently; kody (FLEET_RANK=0) captures the full edge instead of a fraction.
      - ENABLE_FLEET_STAGGER=false
      - FLEET_SIZE=5
      - FLEET_OVERLAP=2
      # B2 market-tide gate: down-size puts entered against the market-wide whale tide (puts-only) — 2026-06-15.
      - ENABLE_V7_TIDE_GATE=true
      - ENABLE_V6_BREAKEVEN_RATCHET=true
      - ENABLE_V6_2PM_TIGHTEN=true
      - ENABLE_V6_PREMIUM_CAP=false
      - ENABLE_OTM_DISTANCE_GATE=false
      - ENABLE_DELTA_GATE=true
      - ENABLE_V6_SPREAD_GATE=true
      - ENABLE_V6_SCALEOUT=true
      - ENABLE_V6_DCA=true
      - ENABLE_V6_EARLY_POP_GATE=true
      - ENABLE_DIP_CONFIRM=true

      - ENABLE_V7_CONVICTION_SIZING=true
      # runner_v1 P(runner) call-sizing — fleet-uniform 2026-06-22 (matches kody validated config).
      - ENABLE_RUNNER_V1_SIZING=true
      - RUNNER_V1_Q1=0.39
      - RUNNER_V1_Q2=0.69
      - RUNNER_V1_Q3=0.85
      - RUNNER_V1_MULT_Q1=0.7
      - RUNNER_V1_MULT_Q2=0.9
      - RUNNER_V1_MULT_Q3=1.1
      - RUNNER_V1_MULT_Q4=1.3
      # conf_linear (winner-concentration) sizing — AGGRESSIVE 0.4-1.8, 6mo-validated 2026-06-22:
      # +107% P&L vs flat at LOWER drawdown over 254 trades. Stacks w/ runner_v1 (×0.28–×2.34);
      # MAX_POSITION_PCT/MAX_POSITION_DOLLARS caps bound the upside. Live kody, paper shadows on the rest.
      - ENABLE_CONF_LINEAR_SIZING=true
      - CONF_LINEAR_BUDGET_MIN=0.4
      - CONF_LINEAR_BUDGET_MAX=1.8
      - AGENT_ID=owlet_yank
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0

    volumes:
      - ./journal/owlet-yank:/app/journal
      - ./journal/models:/app/journal/models:ro

  # ── Signal generation engine — DISABLED ──
  # Each trading bot now runs its own ML scan loop (V3 models) directly in
  # bot_runner.py, matching the gold standard backtest. The standalone sourcing
  # container is no longer needed — bots fetch live Polygon data and run
  # pattern_entry + entry_timing + regime_classifier themselves.
  # owlet-sourcing:
  #   <<: *bot-common
  #   container_name: owlet-sourcing
  #   command: ["python", "-m", "options_owl.sourcing.ml_pipeline"]
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     - PYTHONUNBUFFERED=1
  #     - ENABLE_ML_PIPELINE=true
  #     - ML_PATTERN_THRESHOLD=0.85
  #     - ML_ENTRY_THRESHOLD=0.70
  #     - ML_REGIME_THRESHOLD=0.19
  #     - ML_SCAN_START_MIN=5
  #     - ML_SCAN_END_MIN=90
  #     - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
  #     - AGENT_ID=owlet_sourcing
  #     - ENABLE_REDIS=true
  #     - REDIS_URL=redis://redis:6379/0
  #   volumes:
  #     - ./journal/owlet-sourcing:/app/journal:rw
  #     - ./journal/models:/app/journal/models:ro
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 512M

  # ── Agent Dashboard (web UI for monitoring + controls) ──
  owlet-dashboard:
    <<: *bot-common
    container_name: owlet-dashboard
    command: ["python", "-m", "options_owl.dashboard.app"]
    ports:
      # Bind to localhost ONLY — the dashboard is reached exclusively through the
      # Cloudflare tunnel (https://options-owl.com → cloudflared → localhost:8443).
      # NOT exposed on the public IP (Docker publishes bypass UFW, so this bind is the
      # real access control). To expose publicly again, revert to "0.0.0.0:8443:8443".
      - "127.0.0.1:8443:8443"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      - REDIS_URL=redis://redis:6379/0
      - DASHBOARD_SECRET_KEY=${DASHBOARD_SECRET_KEY:-change-me-in-production}
      - DASHBOARD_DEFAULT_PASSWORD=${DASHBOARD_DEFAULT_PASSWORD:-changeme123}
      - DASHBOARD_PORT=8443
    volumes:
      - ./journal:/app/journal:ro
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; r=httpx.get('http://localhost:8443/health'); assert r.status_code==200"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Centralized data harvester — single source of truth for all market data.
  # Captures: Polygon options chain snapshots, stock candles (/stocks WS),
  # institutional option flow (/options WS), and streams to all bots via Redis.
  # All trading bots have ENABLE_POLYGON_WS=false — they read from Redis/PG.
  owlet-harvester:
    <<: *bot-common
    container_name: owlet-harvester
    command: ["python", "-m", "options_owl.harvester"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      # + new UW-flow CALL tickers (ORCL,INTC,ARM,GOOG,LRCX) so agents get real-time Redis price/
      # candle/premium streams for them (else they fall back to Polygon REST). GOOG ≠ GOOGL.
      - HARVEST_UNIVERSE=SPY,QQQ,IWM,AAPL,TSLA,NVDA,META,AMD,AMZN,GOOGL,MSFT,MU,MSTR,NFLX,SMCI,PLTR,COIN,BA,JPM,AVGO,XLK,XLF,DIA,GLD,SLV,TLT,VIX,ORCL,INTC,ARM,GOOG,LRCX
      - HARVEST_POLL_INTERVAL=15
      - HARVEST_MAX_DTE=7
      - HARVEST_STRIKE_WINDOW=5
      - HARVEST_MARKET_HOURS_ONLY=true
      # PostgreSQL for tick data capture (stock prices, option snapshots, flow)
      - ENABLE_POSTGRES=true
      - DATABASE_URL=postgresql://owl:${POSTGRES_PASSWORD:-owl_dev_2026}@postgres:5432/options_owl
      # Redis for real-time streaming to all trading bots
      - ENABLE_REDIS=true
      - REDIS_URL=redis://redis:6379/0
      # Centralized flow capture — uses Kody's key for /options WS.
      # All bots have ENABLE_POLYGON_WS=false so this key's slot is free.
      - FLOW_POLYGON_API_KEY=${POLYGON_API_KEY}
    volumes:
      - ./journal/owlet-harvester:/app/journal

  # owlet-flow-shadow RETIRED 2026-06-19: the harvester is now the SOLE UW flow-alerts WS holder and
  # publishes qualifying FlowSignals to Redis (owl:flow:signals); all bots consume via
  # subscribe_flow_signals — no per-bot WS, no multi-client connection contention, no redundant shadow.

  # Forward-collects darkpool prints (UW has no history) → journal/darkpool/darkpool.db, for B3
  # (darkpool S/R) research. Observe-only, REST poll every 5min market hours; zero trade impact.
  owlet-darkpool-shadow:
    <<: *bot-common
    container_name: owlet-darkpool-shadow
    command: ["python", "scripts/uw_darkpool_shadow.py"]
    healthcheck:
      disable: true
    volumes:
      - ./journal:/app/journal
